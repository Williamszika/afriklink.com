# Don't deploy these to Vercel (smaller, safer build)
.git
.claude
ai-ops
storage
tests

# Secrets & local env (configure these in the Vercel dashboard instead)
.env

# Phase 0 needs no Composer deps (native autoloader + native .env loader + UUID
# fallback), so skip the Composer build step entirely — the first deploy can't fail
# on dependency resolution. Re-enable (remove these two lines) when real deps land.
composer.json
composer.lock
vendor
